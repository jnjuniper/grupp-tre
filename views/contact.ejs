<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link
      rel="icon"
      href="/images/logo.svg"
      media="(prefers-color-scheme: light)"
    />

    <link
      rel="icon"
      href="/images/logo-white.svg"
      media="(prefers-color-scheme: dark)"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        margin: 0;
        padding: 0;
        background-color: rgb(154, 108, 108);
      }
    </style>
  </head>
  <body class="bg-gray-100 min-h-screen flex flex-col">
    <main class="flex-grow">
      <div id="pageContent">
        <header><%- include('partials/header') %></header>
        <div
          class="w-full flex flex-col gap-4 sm:grid sm:grid-cols-3 md:gap-8 lg-gap-8 lg:grid-cols-7"
        >
          <div class="mt-28 ml-10 lg:col-start-2 lg:col-span-2">
            <h2 class="mb-4 text-2xl font-bold">Contact</h2>
            <p class="mb-2 text-xl">Tel +4670 123 45 67</p>
            <p class="mb-2 text-xl">email@example.com</p>
          </div>
          <form
            id="contactForm"
            class="mx-4 sm:mt-28 sm:mr-10 col-span-2 lg:col-start-4 lg:col-span-3 grid grid-cols-2 p-4 bg-white rounded-lg shadow-lg"
          >
            <div class="col-span-1 mr-4 mt-2">
              <label for="firstName" class="block text-lg">First name:</label>
              <input
                type="text"
                id="firstName"
                name="firstName"
                required
                class="w-full p-2 border border-black rounded focus:outline-none focus:border-blue-500"
              />
            </div>

            <div class="col-span-1 mt-2">
              <label for="lastName" class="block text-lg">Last name:</label>
              <input
                type="text"
                id="lastName"
                name="lastName"
                required
                class="w-full p-2 border border-black rounded focus:outline-none focus:border-blue-500"
              />
            </div>

            <div class="col-span-2 mt-4">
              <label for="email" class="block text-lg">Email:</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full p-2 border border-black rounded focus:outline-none focus:border-blue-500"
              />
            </div>

            <div class="col-span-2 mt-4">
              <label for="subject" class="block text-lg">Subject:</label>
              <input
                type="text"
                id="subject"
                name="subject"
                required
                class="w-full p-2 border border-black rounded focus:outline-none focus:border-blue-500"
              />
            </div>

            <div class="col-span-2 mt-4">
              <label for="description" class="block text-lg">Message:</label>
              <textarea
                id="description"
                name="description"
                rows="5"
                class="w-full p-2 border border-black rounded focus:outline-none focus:border-blue-500"
              ></textarea>
            </div>

            <div class="col-span-2 flex justify-center m-2">
              <button
                type="submit"
                id="submitButton"
                class="px-8 py-3 border border-black bg-white hover:bg-black hover:text-white transition-all text-center"
              >
                Send
              </button>
            </div>
          </form>
        </div>
      </div>
    </main>
    <footer class="mt-14"><%- include('partials/footer') %></footer>

    <!-- Pop-up meddelande -->
    <div
      id="popUp"
      class="hidden fixed inset-0 bg-black bg-opacity-80 flex items-center justify-center p-8"
    >
      <div class="bg-white p-8 rounded-lg shadow-lg text-center">
        <h2 class="text-2xl font-bold mb-4">Thank you for your message!</h2>
        <p class="mb-4 text-xl">
          We have received your message and will get back to you as soon as we
          can.
        </p>
        <button
          id="closeButton"
          class="px-8 py-3 border border-black bg-white hover:bg-black hover:text-white transition-all text-center"
        >
          Close
        </button>
      </div>
    </div>
  </body>
</html>
